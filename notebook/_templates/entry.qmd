---
title: "{{TITLE}}"
id: {{ID}}
date: {{DATE}}
opened: "{{OPENED}}"
closed: ""               # set by `lb.py close`
status: open             # open | closed | discarded
author: claude-code      # claude-code | human
session: "{{SESSION}}"
runs: []                 # e.g. [R-0007, R-0008]
events: []               # documented events, e.g. [E-0012, E-0013]
discarded: {}            # events not worth recording, each with a justification
corrected: ""            # ID of an earlier entry that this one corrects
verdict: ""              # confirmed | refuted | undecided | not-applicable
reviewed_by: ""          # set only by the human
---

<!-- Labels for figures/equations/tables always with slug prefix: fig-{{SLUG}}-name, eq-{{SLUG}}-name -->

## Why

<!-- Occasion, question, relation to earlier entries (@sec-..., IDs), events or plan. -->

## Hypothesis / Expectation

<!-- Write and commit BEFORE the run. Quantitative and falsifiable:
     What is expected, which quantity, which threshold decides? -->

## Method

### Sources

<!-- Reports, commits, earlier runs, issues. -->

### Equations and code references

<!-- Every equation with its implementation location (file:lines) and all symbols defined with units.
     State deviations between formula and code explicitly. Example:

$$
y_{k+1} = y_k + h\, f(t_k, y_k)
$$ {#eq-{{SLUG}}-euler}

implemented in `src/integrate.py:40–52`; $h$ = step size in s, $y$ in the unit of the state vector. -->

### Parameters and test coverage

<!-- Which points of the parameter space were checked? Not just one fiducial point.

| Run | step $h$ | tolerance | Purpose |
|---|--:|--:|---|
| R-0007 | {{</* run-param R-0007 h */>}} | {{</* run-param R-0007 tol */>}} | Fiducial |
-->

## Result

<!-- Observation only, no interpretation. Numbers via {{</* result R-0007 metric */>}} from results.tsv,
     figures only from analysis.py (each .pdf and .png):

![Description. {{</* prov R-0007 */>}}](fig/name){#fig-{{SLUG}}-name}
-->

## Verification

<!-- Checks with outcome (passed/failed, numbers), as applicable: conservation laws, limiting cases
     with known solutions, convergence with respect to resolution/tolerance, comparison with a
     reference or benchmark. -->

## Interpretation

<!-- Interpretation, uncertainties, alternative explanations. Clearly separated from "Result". -->

## Consequences and next steps

## Failed attempts

<!-- Dead ends with the reason, even if no longer visible in the final code. Otherwise "none". -->

## Deviations from plan

<!-- Every deviation from plan/hypothesis/test scope with justification. Otherwise "none". -->
